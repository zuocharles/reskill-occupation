/**
 * Database stub - replace with real database connection
 * when ready to persist onboarding data.
 */
export function getDB() {
  console.warn('[reskill-occupation] Database not configured yet')
  return null
}
